import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const OpenModalReducer = createSlice({
  name: "OpenModalReducer",
  initialState,
  reducers: {

    
  }
});

export const {} = OpenModalReducer.actions

export default OpenModalReducer.reducer